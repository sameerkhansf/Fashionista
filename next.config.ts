import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["images.squarespace-cdn.com", "media.discordapp.net", "picsum.photos", "www.thejacketmaker.com", "www.mrporter.com", "cdn.discordapp.com"],
  },
  /* config options here */
};

export default nextConfig;
